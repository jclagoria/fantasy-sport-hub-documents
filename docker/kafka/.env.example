# ═══════════════════════════════════════════════════
# 🐳 Docker Compose Environment Configuration
# Example configuration for Kafka cluster with Zookeeper and Kafka-UI
# ═══════════════════════════════════════════════════

# ===================================================
# 🏷️ Image Versions
# ===================================================
# Kafka image version (e.g., 7.5.0, latest)
KAFKA_IMAGE_VERSION={{KAFKA_IMAGE_VERSION}}

# Kafka-UI image version (e.g., latest, v0.7.0)
KAFKA_UI_IMAGE_VERSION={{KAFKA_UI_IMAGE_VERSION}}

# ===================================================
# 🏗️ Container Configuration
# ===================================================
# Prefix for all container names
CONTAINER_PREFIX={{CONTAINER_PREFIX}}

# Container names (automatically prefixed)
ZOOKEEPER_CONTAINER_NAME=${CONTAINER_PREFIX}-zookeeper
KAFKA_1_CONTAINER_NAME=${CONTAINER_PREFIX}-kafka-1
KAFKA_2_CONTAINER_NAME=${CONTAINER_PREFIX}-kafka-2
KAFKA_3_CONTAINER_NAME=${CONTAINER_PREFIX}-kafka-3
KAFKA_UI_CONTAINER_NAME=${CONTAINER_PREFIX}-kafka-ui
KAFKA_INIT_CONTAINER_NAME=${CONTAINER_PREFIX}-kafka-init

# ===================================================
# 🌐 Network Configuration
# ===================================================
# Docker network name for Kafka cluster
KAFKA_NETWORK_NAME={{KAFKA_NETWORK_NAME}}

# Network driver (bridge, overlay, etc.)
KAFKA_NETWORK_DRIVER={{KAFKA_NETWORK_DRIVER}}

# ===================================================
# 💾 Volume Configuration
# ===================================================
# Volume names (automatically prefixed)
ZOOKEEPER_DATA_VOLUME=${CONTAINER_PREFIX}-zookeeper-data
ZOOKEEPER_LOGS_VOLUME=${CONTAINER_PREFIX}-zookeeper-logs
KAFKA_1_DATA_VOLUME=${CONTAINER_PREFIX}-kafka-1-data
KAFKA_2_DATA_VOLUME=${CONTAINER_PREFIX}-kafka-2-data
KAFKA_3_DATA_VOLUME=${CONTAINER_PREFIX}-kafka-3-data

# ===================================================
# 🔌 Port Configuration
# ===================================================
# Zookeeper Ports
ZOOKEEPER_CLIENT_PORT={{ZOOKEEPER_CLIENT_PORT}}
ZOOKEEPER_EXTERNAL_PORT={{ZOOKEEPER_EXTERNAL_PORT}}

# Kafka Broker 1 Ports
KAFKA_1_INTERNAL_PORT={{KAFKA_1_INTERNAL_PORT}}
KAFKA_1_EXTERNAL_PORT={{KAFKA_1_EXTERNAL_PORT}}
KAFKA_1_HOST_EXTERNAL_PORT={{KAFKA_1_HOST_EXTERNAL_PORT}}

# Kafka Broker 2 Ports
KAFKA_2_INTERNAL_PORT={{KAFKA_2_INTERNAL_PORT}}
KAFKA_2_EXTERNAL_PORT={{KAFKA_2_EXTERNAL_PORT}}
KAFKA_2_HOST_EXTERNAL_PORT={{KAFKA_2_HOST_EXTERNAL_PORT}}

# Kafka Broker 3 Ports
KAFKA_3_INTERNAL_PORT={{KAFKA_3_INTERNAL_PORT}}
KAFKA_3_EXTERNAL_PORT={{KAFKA_3_EXTERNAL_PORT}}
KAFKA_3_HOST_EXTERNAL_PORT={{KAFKA_3_HOST_EXTERNAL_PORT}}

# Kafka UI Port
KAFKA_UI_PORT={{KAFKA_UI_PORT}}

# ===================================================
# 🐘 Zookeeper Configuration
# ===================================================
# Zookeeper tick time in milliseconds
ZOOKEEPER_TICK_TIME={{ZOOKEEPER_TICK_TIME}}

# Health Check Settings
ZOOKEEPER_HEALTHCHECK_INTERVAL={{ZOOKEEPER_HEALTHCHECK_INTERVAL}}
ZOOKEEPER_HEALTHCHECK_TIMEOUT={{ZOOKEEPER_HEALTHCHECK_TIMEOUT}}
ZOOKEEPER_HEALTHCHECK_RETRIES={{ZOOKEEPER_HEALTHCHECK_RETRIES}}

# ===================================================
# ⚡ Kafka Broker Configuration
# ===================================================
# Broker IDs (must be unique for each broker)
KAFKA_BROKER_1_ID={{KAFKA_BROKER_1_ID}}
KAFKA_BROKER_2_ID={{KAFKA_BROKER_2_ID}}
KAFKA_BROKER_3_ID={{KAFKA_BROKER_3_ID}}

# Zookeeper Connection (internal Docker DNS)
KAFKA_ZOOKEEPER_CONNECT=zookeeper:${ZOOKEEPER_CLIENT_PORT}

# Replication & ISR Configuration
KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR={{KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR}}
KAFKA_TRANSACTION_STATE_LOG_MIN_ISR={{KAFKA_TRANSACTION_STATE_LOG_MIN_ISR}}
KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR={{KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR}}
KAFKA_MIN_INSYNC_REPLICAS={{KAFKA_MIN_INSYNC_REPLICAS}}

# Topic Configuration
KAFKA_AUTO_CREATE_TOPICS_ENABLE={{KAFKA_AUTO_CREATE_TOPICS_ENABLE}}
KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS={{KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS}}

# Performance Settings
KAFKA_COMPRESSION_TYPE={{KAFKA_COMPRESSION_TYPE}}

# Retention Policy (in milliseconds, default 7 days)
KAFKA_LOG_RETENTION_MS={{KAFKA_LOG_RETENTION_MS}}

# Listener Security Protocol
KAFKA_LISTENER_SECURITY_PROTOCOL_MAP={{KAFKA_LISTENER_SECURITY_PROTOCOL_MAP}}

# Health Check Settings
KAFKA_HEALTHCHECK_INTERVAL={{KAFKA_HEALTHCHECK_INTERVAL}}
KAFKA_HEALTHCHECK_TIMEOUT={{KAFKA_HEALTHCHECK_TIMEOUT}}
KAFKA_HEALTHCHECK_RETRIES={{KAFKA_HEALTHCHECK_RETRIES}}

# ===================================================
# 📊 Kafka UI Configuration
# ===================================================
# Display name for the Kafka cluster in the UI
KAFKA_CLUSTER_NAME={{KAFKA_CLUSTER_NAME}}

# Bootstrap Servers (internal network addresses)
KAFKA_BOOTSTRAP_SERVERS=kafka-1:${KAFKA_1_INTERNAL_PORT},kafka-2:${KAFKA_2_INTERNAL_PORT},kafka-3:${KAFKA_3_INTERNAL_PORT}

# ===================================================
# 🚀 Initialization Configuration
# ===================================================
# Wait time for Kafka initialization (seconds)
KAFKA_INIT_WAIT_TIME={{KAFKA_INIT_WAIT_TIME}}

# Path to Kafka scripts
KAFKA_SCRIPTS_PATH={{KAFKA_SCRIPTS_PATH}}

# ===================================================
# 📝 Notes
# ===================================================
# To use this file:
# 1. Copy this file to .env in the same directory
# 2. Replace all {{VARIABLE_NAME}} placeholders with actual values
# 3. Update docker-compose.kafka.yml to reference these variables
# 4. Run: docker-compose --env-file .env -f docker-compose.kafka.yml up

# Note: The following variables are dynamically constructed in the docker-compose file:
# - KAFKA_1_ADVERTISED_LISTENERS
# - KAFKA_2_ADVERTISED_LISTENERS
# - KAFKA_3_ADVERTISED_LISTENERS
