# ===================================================
# 🗄️ MongoDB Configuration
# ===================================================
# Root username for MongoDB (has full admin access)
MONGO_INITDB_ROOT_USERNAME={{MONGO_INITDB_ROOT_USERNAME}}

# Root password for MongoDB (change this in production!)
MONGO_INITDB_ROOT_PASSWORD={{MONGO_INITDB_ROOT_PASSWORD}}

# Default database to create on initialization
MONGO_INITDB_DATABASE={{MONGO_INITDB_DATABASE}}

# ===================================================
# 🌐 MongoDB Express Configuration (Web UI)
# ===================================================
# MongoDB server hostname (use service name in Docker)
ME_CONFIG_MONGODB_SERVER={{ME_CONFIG_MONGODB_SERVER}}

# MongoDB server port (default: 27017)
ME_CONFIG_MONGODB_PORT={{ME_CONFIG_MONGODB_PORT}}

# Admin username for MongoDB Express (should match MONGO_INITDB_ROOT_USERNAME)
ME_CONFIG_MONGODB_ADMINUSERNAME={{ME_CONFIG_MONGODB_ADMINUSERNAME}}

# Admin password for MongoDB Express (should match MONGO_INITDB_ROOT_PASSWORD)
ME_CONFIG_MONGODB_ADMINPASSWORD={{ME_CONFIG_MONGODB_ADMINPASSWORD}}

# Enable basic authentication for MongoDB Express (true/false)
ME_CONFIG_BASICAUTH={{ME_CONFIG_BASICAUTH}}

# Username for MongoDB Express web interface
ME_CONFIG_BASICAUTH_USERNAME={{ME_CONFIG_BASICAUTH_USERNAME}}

# Password for MongoDB Express web interface
ME_CONFIG_BASICAUTH_PASSWORD={{ME_CONFIG_BASICAUTH_PASSWORD}}

# ===================================================
# 🔌 Port Configuration
# ===================================================
# Port to expose MongoDB on the host machine (default: 27017)
MONGO_PORT={{MONGO_PORT}}

# Port to expose MongoDB Express web interface (default: 8081)
MONGO_EXPRESS_PORT={{MONGO_EXPRESS_PORT}}

# ===================================================
# 📝 Additional Configuration (Optional)
# ===================================================
# Uncomment and configure these if needed:

# Authentication database (default: admin)
# MONGO_AUTH_SOURCE=admin

# Enable MongoDB replica set (true/false)
# MONGO_REPLICA_SET_MODE=false

# Replica set name (required if REPLICA_SET_MODE=true)
# MONGO_REPLICA_SET_NAME=rs0

# Key file for replica set authentication (required for replica sets)
# MONGO_REPLICA_SET_KEY_FILE=/data/configdb/keyfile

# ===================================================
# 🚀 Usage Instructions
# ===================================================
# 1. Copy this file to .env in the same directory
# 2. Replace all {{VARIABLE_NAME}} placeholders with actual values
# 3. Update your docker-compose.yml to use these environment variables
# 4. Run: docker-compose up -d
#
# Note: For production environments:
# - Use strong, unique passwords
# - Enable authentication
# - Consider using a secrets management solution
# - Configure proper network isolation
